{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.10.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"nvidiaTeslaT4","dataSources":[{"sourceId":1650695,"sourceType":"datasetVersion","datasetId":976194},{"sourceId":6374376,"sourceType":"datasetVersion","datasetId":3673013},{"sourceId":8968421,"sourceType":"datasetVersion","datasetId":4066836}],"dockerImageVersionId":30528,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":true}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"markdown","source":"<center style=\"border-radius:10px;\npadding: 3rem 2rem;\nborder: 3px solid #F54257;\n\">\n<h1 style=\"color:#F54257; \nfont-size:3.0rem;\nmargin:0;\n\">KITTI Object Detection</h1>\n<h2 style=\"color:#F54257; \nfont-size:2.0rem;\nmargin-top:1rem;\nmargin-bottom:2.5rem;\n\">YOLOv8n | Ultralytics</h2>\n<a href=\"https://kaggle.com/shreydan\" style=\"color: white;\nbackground-color: #F54257;\nborder-radius: 25px;\npadding: 1rem 1.5rem;\ntext-decoration: none;\n\">@shreydan</a>\n</center>","metadata":{}},{"cell_type":"markdown","source":"# Imports\n<div style=\"width:100%;height:0;border-bottom: 3px solid #F03A4F;margin-bottom: 1rem;\"></div>","metadata":{}},{"cell_type":"code","source":"!pip install ultralytics -q","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"%env WANDB_DISABLED=True","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"from ultralytics import YOLO\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nfrom pathlib import Path\nimport json\nfrom sklearn.model_selection import train_test_split\nfrom tqdm.auto import tqdm\nimport shutil\nfrom PIL import Image","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"# Preparing Dataset\n<div style=\"width:100%;height:0;border-bottom: 3px solid #F27B40;margin-bottom: 1rem;\"></div>","metadata":{}},{"cell_type":"code","source":"base_dir = Path('/kaggle/input/kitti-dataset')\nimg_path = base_dir / 'data_object_image_2' / 'training' / 'image_2'\nlabel_path = Path('/kaggle/input/kitti-dataset-yolo-format/labels')\nwith open('/kaggle/input/kitti-dataset-yolo-format/classes.json','r') as f:\n    classes = json.load(f)\n\nclasses","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"ims = sorted(list(img_path.glob('*')))\nlabels = sorted(list(label_path.glob('*')))\npairs = list(zip(ims,labels))\npairs[:2]","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"# Preparing File Structure\n<div style=\"width:100%;height:0;border-bottom: 3px solid #F4B343;margin-bottom: 1rem;\"></div>\n\n```\n/kaggle/working\n    |\n    -train\n    |   |\n    |   -000000.png\n    |   -000000.txt\n    |   ...\n    |\n    -val\n      |\n      -000001.png\n      -000001.txt\n      ...\n```","metadata":{}},{"cell_type":"code","source":"train, test = train_test_split(pairs,test_size=0.1,shuffle=True)\nlen(train), len(test)","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"train_path = Path('train').resolve()\ntrain_path.mkdir(exist_ok=True)\nvalid_path = Path('valid').resolve()\nvalid_path.mkdir(exist_ok=True)","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"for t_img, t_lb in tqdm(train):\n    im_path = train_path / t_img.name\n    lb_path = train_path / t_lb.name\n    shutil.copy(t_img,im_path)\n    shutil.copy(t_lb,lb_path)","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"for t_img, t_lb in tqdm(test):\n    im_path = valid_path / t_img.name\n    lb_path = valid_path / t_lb.name\n    shutil.copy(t_img,im_path)\n    shutil.copy(t_lb,lb_path)","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"# YAML file for the data\n<div style=\"width:100%;height:0;border-bottom: 3px solid #F4CE45;margin-bottom: 1rem;\"></div>","metadata":{}},{"cell_type":"code","source":"yaml_file = 'names:\\n'\nyaml_file += '\\n'.join(f'- {c}' for c in classes)\nyaml_file += f'\\nnc: {len(classes)}'\nyaml_file += f'\\ntrain: {str(train_path)}\\nval: {str(valid_path)}'\nwith open('kitti.yaml','w') as f:\n    f.write(yaml_file)","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"#  验证图片数量\ndef count_imgs(path):\n    return len(glob.glob(os.path.join(path, '*.png')) +\n               glob.glob(os.path.join(path, '*.jpg')))","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import yaml, os, glob\n\nprint(\"========== kitti训练样本数量 ==========\")\nprint(f\"train: {count_imgs('/kaggle/working/train')} 张\")\nprint(f\"valid: {count_imgs('/kaggle/working/valid')} 张\")\n!cat kitti.yaml","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"##  显示cardetection数据集的类型\n/kaggle/input/cardetection/car/data.yaml","metadata":{}},{"cell_type":"code","source":"tsd_yaml_path = \"/kaggle/input/cardetection/car/data.yaml\"\n!cat \"{tsd_yaml_path}\"","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"# 合并 kitti.yaml 与 TSD data.yaml 为 kitti-tsd.yaml","metadata":{}},{"cell_type":"code","source":"import yaml\nimport os\nimport glob\n\n# 1. 读取原始类别\nwith open('/kaggle/working/kitti.yaml') as f:\n    kitti = yaml.safe_load(f)\nwith open('/kaggle/input/cardetection/car/data.yaml') as f:\n    tsd = yaml.safe_load(f)\n\n# 2. 合并类别（Kitti 在前，TSD 在后）\nnames = kitti['names'] + [n for n in tsd['names'] if n not in kitti['names']]\nnc = len(names)\n\n# 3. 构造最终 yaml（包含 Kitti 和 TSD 的路径）\nmerged = {\n    'train': [\n        '/kaggle/working/train',  # Kitti 的 train 数据\n        '/kaggle/input/cardetection/car/train/images'  # TSD 的 train 数据\n    ],\n    'val': [\n        '/kaggle/working/valid',  # Kitti 的 val 数据\n        '/kaggle/input/cardetection/car/valid/images'  # TSD 的 val 数据\n    ],\n    'test': '',  # 如无 test，可留空或删除\n    'nc': nc,\n    'names': names\n}\n\n# 4. 写出文件\noutput_path = '/kaggle/working/kitti-tsd.yaml'\nwith open(output_path, 'w') as f:\n    yaml.dump(merged, f, sort_keys=False)\n\n# 5. 打印验证\ndef count_imgs(paths):\n    exts = ('*.jpg', '*.jpeg', '*.png')\n    return sum(len(glob.glob(os.path.join(p, ext))) for p in paths for ext in exts)\n\nprint(\"========== 合并后实际数量 ==========\")\nprint(f\"train: {count_imgs(merged['train'])} 张\")\nprint(f\"valid: {count_imgs(merged['val'])} 张\")\n\nprint(\"\\n✅ kitti-tsd.yaml 已生成：\")\n!cat /kaggle/working/kitti-tsd.yaml","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"\n","metadata":{}},{"cell_type":"markdown","source":"# Model\n<div style=\"width:100%;height:0;border-bottom: 3px solid #F27B40;margin-bottom: 1rem;\"></div>","metadata":{}},{"cell_type":"code","source":"model = YOLO('yolov8n.yaml')\nmodel = YOLO('yolov8n.pt')","metadata":{"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"},"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"# Training\n<div style=\"width:100%;height:0;border-bottom: 3px solid #F5E947;margin-bottom: 1rem;\"></div>","metadata":{}},{"cell_type":"markdown","source":"## 迁移学习策略\n\n冻结层：在迁移学习的初始阶段，可以冻结预训练模型的部分层，只训练最后几层。这有助于在不破坏预训练模型对原始任务的性能的情况下，逐步调整模型以适应新任务。\n例如，冻结除最后一层外的所有层，训练 20 个 epoch，然后解冻所有层继续训练。","metadata":{}},{"cell_type":"markdown","source":"### 冻结参数的训练方法","metadata":{}},{"cell_type":"markdown","source":"## 第一阶段训练","metadata":{}},{"cell_type":"code","source":"# 确保模型处于训练模式\nmodel.train()\n\n# 冻结所有层\nfor param in model.model.parameters():\n    param.requires_grad = False\n\n# 解冻 Detect 模块\ndetect_module = model.model.model[-1]  # Detect 模块是 model.model 的最后一个模块\nfor param in detect_module.parameters():\n    param.requires_grad = True\n\n# 第一阶段训练参数\ntrain_results = model.train(\n    data='/kaggle/working/kitti-tsd.yaml', \n    epochs=20,  # 增加训练轮数\n    patience=5,  # 增加早停机制的耐心\n    mixup=0.3,  # 增加 Mixup 的比例\n    mosaic=0.7,  # 增加 Mosaic 的比例\n    lr0=0.005,  # 使用较小的初始学习率\n    lrf=0.2,  # 保持最终学习率不变\n    batch=64,  # 增加批量大小\n    project='yolov8n-kitti-tsd',\n    device=\"cuda\",\n    cos_lr=True,  # 使用余弦学习率衰减\n    weight_decay=0.0005,  # 添加权重衰减\n)","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"## 第二阶段训练","metadata":{}},{"cell_type":"code","source":"# 解冻所有层\nfor param in model.model.parameters():\n    param.requires_grad = True\n\n# 第二阶段训练参数\ntrain_results = model.train(\n    data='/kaggle/working/kitti-tsd.yaml',     # Path to the dataset configuration file\n    epochs=80,                # Number of epochs to train for\n    imgsz=640,                 # Size of input images as integer\n    device=0,                  # Device to run on, i.e. cuda device=0 \n    patience=20,               # Epochs to wait for no observable improvement for early stopping of training\n    batch=32,                  # Number of images per batch\n    optimizer='auto',          # Optimizer to use, choices=[SGD, Adam, Adamax, AdamW, NAdam, RAdam, RMSProp, auto]\n    lr0=0.0001,                 # Initial learning rate \n    lrf=0.1,                   # Final learning rate (lr0 * lrf)\n    dropout=0.1,               # Use dropout regularization\n    seed=0                     # Random seed for reproducibility\n)\n","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### 不冻结参数的训练方法","metadata":{}},{"cell_type":"code","source":"\"\"\"\ntrain_results = model.train(\n    data='/kaggle/working/kitti-tsd.yaml', \n    epochs=100,\n    patience=5,\n    mixup=0.2,\n    mosaic=0.5,\n    lr0=0.01,\n    lrf=0.2,\n    batch=16,\n    project='yolov8n-kitti-tsd',\n    device=0,\n)\n\"\"\"","metadata":{"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"},"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"## 及时将训练好的模型保存到 “/kaggle/working/” 目录下","metadata":{}},{"cell_type":"code","source":"from datetime import datetime\n\n# 获取当前日期和时间\ncurrent_time = datetime.now().strftime(\"%Y-%m-%d_%H-%M-%S\")\n\n# 保存模型到包含日期和时间的文件名\nmodel.save(f'/kaggle/working/best_model_{current_time}.pt')","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"# Validation\n<div style=\"width:100%;height:0;border-bottom: 3px solid #E7F549;margin-bottom: 1rem;\"></div>","metadata":{}},{"cell_type":"code","source":"valid_results = model.val()","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.294Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"##  压缩输出结果，便于下载\n\n将整个训练输出文件夹 yolov8n-kitti 递归压缩成 results.zip，便于一次性下载全部结果（权重、日志、图表等）。","metadata":{}},{"cell_type":"code","source":"!zip -r kitti-tsd-results.zip yolov8n-kitti-tsd","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.295Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"# Results\n<div style=\"width:100%;height:0;border-bottom: 3px solid #F25F3E;margin-bottom: 1rem;\"></div>","metadata":{}},{"cell_type":"code","source":"plt.figure(figsize=(10,20))\nplt.imshow(Image.open('/kaggle/working/yolov8n-kitti-tsd/train/results.png'))\nplt.axis('off')\nplt.show()","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.295Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"plt.figure(figsize=(10,20))\nplt.imshow(Image.open('/kaggle/working/yolov8n-kitti-tsd/train/confusion_matrix.png'))\nplt.axis('off')\nplt.show()","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.295Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"# Predictions\n<div style=\"width:100%;height:0;border-bottom: 3px solid #CEF64B;margin-bottom: 1rem;\"></div>","metadata":{}},{"cell_type":"code","source":"preds = model.predict([test[idx][0] for idx in np.random.randint(0,len(test),(20,))],save=True)","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.295Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import cv2\nfrom IPython.display import HTML\nfrom base64 import b64encode\nimport logging\n\n# 设置日志级别为 WARNING 或更高\nlogging.basicConfig(level=logging.WARNING)\n\n# 1. 加载模型\nmodel_path = r\"/kaggle/working/yolov8n-kitti-tsd/train/weights/best.pt\"\nmodel = YOLO(model_path)\n\n# 2. 读取视频\nvideo_path = '/kaggle/input/cardetection/video.mp4'\ncap = cv2.VideoCapture(video_path)\n\n# 3. 保存推理结果的视频\noutput_path = '/kaggle/working/output_video.mp4'\nfourcc = cv2.VideoWriter_fourcc(*'mp4v')\nfps = cap.get(cv2.CAP_PROP_FPS)\n\n# 降低帧率（例如，降低到原始帧率的一半）\nslower_fps = fps / 2\n\n\nframe_width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))\nframe_height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))\nout = cv2.VideoWriter(output_path, fourcc, slower_fps, (frame_width, frame_height))\n\n# 4. 对视频进行推理并绘制结果\nwhile cap.isOpened():\n    ret, frame = cap.read()\n    if not ret:\n        break\n\n    # 推理\n    results = model(frame)\n\n    # 绘制结果\n    annotated_frame = results[0].plot()\n    out.write(annotated_frame)\n\ncap.release()\nout.release()","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.295Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# 转换视频格式（如果需要）\ninput_video_path = '/kaggle/working/output_video.mp4'\noutput_video_path = '/kaggle/working/output_video_display.mp4'\n\n# 使用 ffmpeg 转换视频格式\n!ffmpeg -i {input_video_path} -vcodec libx264 -acodec aac {output_video_path} -y","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.295Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"## 展示视频","metadata":{}},{"cell_type":"code","source":"def display_video(path):\n    # 读取视频文件\n    video = open(path, 'rb').read()\n    # 编码为 base64\n    video_encoded = b64encode(video).decode('utf-8')\n    # 创建 HTML5 视频标签\n    video_tag = f'<video controls alt=\"test\" src=\"data:video/mp4;base64,{video_encoded}\"></video>'\n    # 返回 HTML 对象\n    return HTML(data=video_tag)\n\n# 调用展示视频\ndisplay_video(output_video_path)","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.295Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"## cleanup","metadata":{}},{"cell_type":"markdown","source":"把训练得到的最佳权重文件复制到工作根目录，方便后续直接加载或下载。","metadata":{}},{"cell_type":"code","source":"!cp /kaggle/working/yolov8n-kitti-tsd/train/weights/best.pt /kaggle/working/yolov8n_ktti_tsd.pt","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.295Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# 列出 /kaggle/working/ 文件夹中的所有文件\nfiles = os.listdir('/kaggle/working/')\nprint(\"当前 /kaggle/working/ 文件夹中的文件：\", files)","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.295Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"将整个训练输出文件夹 yolov8n-kitti 递归压缩成 results.zip，便于一次性下载全部结果（权重、日志、图表等）。","metadata":{}},{"cell_type":"code","source":"!zip -r kitti-tsd-results.zip yolov8n-kitti-tsd","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.295Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"强制删除以下文件/目录，释放磁盘空间","metadata":{}},{"cell_type":"code","source":"!rm -rf yolov8n-kitti-tsd wandb train.cache valid.cache yolov8n.pt train valid","metadata":{"trusted":true,"execution":{"execution_failed":"2025-07-27T12:29:43.295Z"}},"outputs":[],"execution_count":null}]}